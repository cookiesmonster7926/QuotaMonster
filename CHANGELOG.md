# 變更紀錄

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版本號依循 [語意化版本](https://semver.org/lang/zh-TW/)。

## [未發布]

## [0.2.0] — 2026-09-22

第二個公開版本。主題是**把畫面上的每一句話變成有證據的**，
外加一個沒有人量過、量了才發現的效能洞。

### 新增

- **額度底下多了一張圖，兩種樣式可選**（偏好設定第五格）。
  - **每日長條** —— 這個 7 天窗口裡每一天燒了多少。
    ⚠️ 帳號是共用的（別人、別的裝置都算在同一個 7 天裡），而我們只在**這台機器
    正在渲染狀態列**的時候才看得到那個數字。所以每一根長條都帶著信心：
    日界兩端都被看著才是實心，只有一端被看著是淡的，整天沒看到就畫虛線空框。
    〔實測〕18.8 小時裡有 16.1 小時（85%）這台機器沒在渲染，而 app 是醒著的 ——
    用「app 醒著」當證據會把「別人半夜燒掉 30%」那天畫成 0% 且滿分信心。
  - **累計曲線** —— 從窗口起點爬到現在。它**沒有歸屬問題**（不需要分天），
    所以共用帳號完全不影響它。y 軸固定 0–100%，那條斜線是「剛好在重置時用完」。
    ⚠️ 累計值不可能下降，讀到下降就是兩筆讀數有一筆是錯的 ——
    那一段畫成淡色虛線並標明「讀數互相矛盾」，不夾成遞增（那是把壞資料改成好資料）。
- **一般 Agent subagent 的真實下場**：面板收合那一行從「N 個 agent 已結束」
  變成「已完成 N · 失敗 M · 狀態不明 K」，與 workflow 那一行同一套用詞與顏色。
  ⚠️ **「狀態不明」不會被併進「已完成」** —— 實測 18.5% 的背景 agent 從來沒有終端記錄。
- **session 列只留「還沒好的」**，其餘收成一行，並標出是 workflow（WF）還是 agent（AG）。
- **session 的名字用你真正看過的那一個**，不是 Claude Code 湊的佔位名；同名時接上唯一鍵。
- 診斷：`--bench-refresh`（量一拍 `refresh()` 要多久）、
  `--render-panel --chart daily|cumulative`（只覆寫記憶體，不碰你的設定檔）。

### 效能

- **一拍 `refresh()` 從 740 毫秒降到 62.8 毫秒**（一拍的預算是 3000 毫秒）。
  原因是 `AgentTreeBuilder` 每 3 秒把整份母 transcript 讀進來重新解析一次 ——
  這台機器上最大的 22.4MB。改成「記住 `(inode, offset)`、只解析新增的行」。
  ⚠️ 拆開來看最貴的不是 I/O（1–7%）也不是 JSON —— 是**切行**：
  在多 MB 的 Swift 字串上找換行要走 Unicode 的字素邊界，佔掉 68–78%。
  所以要省的是**逐行的工作**，只快取 bytes 一點都不會比較快。
  第一次啟動仍然要補課（實測 399 毫秒），之後每拍接近零。

### 修正

- **選單列的兩條弧各自上色。** 5 小時燒到一半（綠）而 7 天還剩三分之二（藍）時，
  原本兩條都畫成綠的 —— 看起來像 7 天也快沒了。面板的三條進度條一直是各自上色的，
  所以那是同一個事實在兩個地方用了兩種語彙。
- **偏好設定在 `DataStore.init` 就讀進來。** 四個建立點裡有三個不走 `start()`，
  於是 `--render-panel` / `--render-prefs` / `--probe-popover` **從來沒有反映過任何偏好**。
  最糟的是 `--render-prefs`：它的工作就是畫偏好視窗給人看，畫的卻一直是預設值，
  而且還印著「這張圖用的是目前實際的偏好檔」。
- 一般 agent 的「在不在跑」改用 transcript 活動的代理量測（120 秒窗口），
  底部那條沒有資訊的暗軌拿掉。
- `isFresh` 補上未來時間戳的上界 —— mtime 在 2099 的檔案原本會**永遠**通過存活閘。
- tee 的 shadow log 補上 1MB 輪替：我們寫進你磁碟的每一個檔案都要有上限。
- 修掉一則在 CI 的工具鏈上編不過的測試（`cos`/`sin` 對 `CGFloat` 是歧義的）。

### 已知限制（更新）

- **T2（扇出排空的提示音）仍然不對一般 Agent 扇出生效，但理由整個換了。**
  0.1.0 說的是「沒有正面證據」——**那個理由已經不成立**，證據讀得到了。
  現在的理由是〔實測〕**沒有可用的扇出單位**：四個候選鍵給出同一個直方圖
  `{1: 37}`（一個批次都沒有），唯一分得出批次的 `promptId` 跨 transcript 不唯一、
  而且會在 `--resume` 時靜靜消失；加上 18.5% 的背景 agent 從來不終結，
  以及這台機器有史以來只有 6 個真正的扇出批次。
  **讀到的下場只拿來顯示，不拿來發聲** —— 顯示錯了看得見，發聲錯了是一聲謊報。
- **每日長條圖要幾天才會填滿。** 取樣是事件驅動的：不跑 Claude Code 就沒有點。
  （0.1.0 說「那根長條代表什麼還沒有答案」—— 答案是「跟著額度窗口切」，
  所以它是 14:00 到 14:00，不是午夜到午夜。）
- **沒有經過 Apple 公證**，而且做不到（公證要 Developer ID，這個專案只有免費 Apple ID）。
  第一次打開會被 Gatekeeper 擋，處理方式見下載頁。
- **只有 Apple Silicon**（`lipo -info` → arm64）。這是刻意的取捨。
- **macOS 14 / 15 / 16 上沒有人跑過**。binary 的 `minos` 是 14.0，但開發機是 macOS 27。〔推論，非實測〕
- workflow 的 `journal.jsonl` 仍然每拍整份重讀（而且是在新鮮度閘之前）——
  它是剩下那 62.8 毫秒的主要來源。機器已經有了，還沒接上去。


## [0.1.0] — 2026-09-21

第一個公開版本。

### 新增

- **選單列圖示**就是儀表：外弧＝5 小時窗口、內弧＝7 天窗口、底部的點＝幾個 agent 在跑。
  沒有讀數或讀數過期時**不上色**（單色代表「這個數字不可信」）。
- **有人在等你輸入**時圖示換成完全不同的形狀（琥珀色滿環 + 箭頭）並開始呼吸。
  琥珀色在這個 app 裡只有這一個意思。
- **完成訊號**：長工作結束後生物染成丁香紫，滿色 180 秒、退色到 600 秒。
- **面板**：額度三欄（5 小時 / 7 天 / 分模型）、session 列表、context 壓力、
  agent 樹與 workflow 進度、失敗計數。
- **通知**（T1 等你輸入 / T2 扇出排空 / T3 額度分級變化），含在場判斷
  （螢幕鎖定、螢幕休眠、閒置超過 5 分鐘就不出聲）與每小時三則的滾動預算。
- **statusline tee**：可安裝、可還原的 wrapper，把額度讀數從「16 小時可能不更新」
  變成秒級。只動 `settings.json` 裡一個字串，動手前留備份。
- **開機自動啟動**（`SMAppService`）。
- **偏好設定**四項：音效、「明早」幾點、context 黃/紅門檻、額度緊張門檻。
- **額度時間序列**（`usage-history.jsonl`），30 天保留。
- 一整套診斷指令（`--dump`、`--render-panel`、`--render`、`--render-icon`、
  `--trace-alerts`、`--probe-login` …）。

### 已知限制

- **Apple Silicon 專屬**，不出 Intel 版本。
- **沒有經過 Apple 公證** —— 作者只有免費 Apple ID。下載的 DMG 會被 Gatekeeper 擋，
  安裝步驟見 README。
- `minos` 是 macOS 14.0，但〔推論，非實測〕只在 macOS 27 上實際跑過。
- 純 Agent 扇出（不經 workflow）永遠不會觸發 T2 通知 —— 那種 run 沒有終結狀態可讀。
- 讀不到 Focus / 勿擾模式（TCC 不開放），所以在場判斷只看鎖定 / 休眠 / 閒置。
- osascript 送出的通知永遠被歸給「指令碼編輯器」，這是系統行為，改不了。
- **每日長條圖還沒畫。** 不是資料不夠：取樣是事件驅動的（不跑 Claude Code 就沒有點），
  而且 7 天窗口是滾動的、5 小時窗口每五小時歸零 —— 兩條線都不能直接差分成
  「今天用了多少」。「那根長條代表什麼」這個問題還沒有答案。

[未發布]: https://github.com/cookiesmonster7926/QuotaMonster/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/cookiesmonster7926/QuotaMonster/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/cookiesmonster7926/QuotaMonster/releases/tag/v0.1.0
